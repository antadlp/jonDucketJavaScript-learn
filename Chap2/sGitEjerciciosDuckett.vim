!git add -A .
!git commit -m "The output for numeric-variable does not looks like the result in the book becouse it required another components which in the cronology of the book still we need to learn: finished till second example"
!git add -A .
!git push origin master

